# Coreweaver Flagship QA Record

## Desktop preview — 2026-08-21

The local Astro preview rendered the rebuilt homepage successfully at desktop width. The primary navigation, hero hierarchy, signal-state diagram, four-layer system, intervention grid, working method, operating principles, and working-session conversion surface were all present and readable. The public copy reflects the evidence-bounded growth-infrastructure brief; the prior governance-performance, BFT, compliance, and cryptographic-operational claims are absent from the visible homepage.

The first browser-preview attempt was blocked by Vite host allowlisting. The Astro development configuration was then updated to allow the temporary preview host solely for browser QA, and the restarted preview loaded normally. Full mobile, lower-page, legacy-route, link, and metadata checks remain open.

## Working-note route inspection — 2026-08-21

The `/mandate` and `/vaas` preview routes both rendered under the shared flagship header and footer at desktop width. The authority page makes its proposed-method, non-product, non-certification, and non-legal boundary explicit before presenting five practical design controls. The verification page distinguishes observation, review, and assertion; rejects certification, compliance, legal, and performance claims; and directs visitors to AURE’s separate public standard without conflating either product’s status. Both pages retain one clear working-session path and did not show legacy BFT, swarm, ledger, CSRD, or cryptographic-service claims in their browser-visible content.

## Field-notes and local route checks — 2026-08-21

The `/blog` preview rendered a coherent field-notes surface even though the local Notion credentials are intentionally unavailable. Its empty state explicitly says that the library remains quiet until a reviewed source is ready, avoiding fabricated article content or runtime errors. Local route checks returned HTTP 200 for `/`, `/blog`, `/mandate`, `/vaas`, `/llms.txt`, `/llms-full.txt`, and `/schema.json`.

The public claim regression suite passes two tests: it rejects legacy technical, performance, and compliance assertions on the home, working-note, layout, and machine-readable surfaces; and it confirms that the four public operating layers and explicit claim boundary remain visible in `llms.txt`. The production Astro build also passes. The only build-time limitation is expected locally: no Notion token or blog-database identifier was provided, so no private or invented posts are included in the local static preview.

## Brass-and-teal identity and source-map QA — 2026-08-21

The supplied Coreweaver reference identity was translated into a semantic interface palette: **obsidian** for the reading field, **smoked graphite** for raised surfaces, **brass** for decisive actions and authority cues, **restrained teal** for signal state and method cues, and **soft steel** for editorial text contrast. The approved supplied organization-mark file is now the public structured-data logo and default social/fallback asset; the interface still uses the stable geometric header mark for small-scale navigation continuity.

Settled-state screenshot inspection at 1440px and 390px confirmed that the brass working-session action, teal retrieval headline, teal signal path, brass status/node cues, nav treatment, and reading contrast remain legible. The mobile header retains the brand and session action without horizontal overflow. The new `/source-map` route incorporates only compatible GEO Made Simple patterns: descriptive crawlable relationship links, public method/evidence boundaries, a source-aware editorial posture, and a private next step. It does not import that repository’s commercial offers, case studies, pricing, automation, or performance claims.

The `/source-map` route was additionally inspected at settled desktop and 390px mobile states. The page preserves the responsive header treatment, readable brass-and-teal contrast, clear hero hierarchy, and ordinary-HTML public-record framing. No source-map copy overstates the method as a certification, service outcome, technical deployment, or proof record.

## Accessibility and conversion-boundary checks — 2026-08-21

The release-contract suite now verifies the shared skip link and `main` target, a visible `:focus-visible` treatment, a `prefers-reduced-motion` safeguard, and a primary `h1` on every flagship method surface. The public conversion boundary is intentionally **mailto-only**: the home, authority, and source-map actions open a contextual working-session email; no client-side collection form, private intake store, or invisible submission mechanism is present. The verification-limits page deliberately hands interested visitors to AURE’s separate public standard rather than treating that route as a Coreweaver inquiry form. This boundary is tested in the four-test public-surface suite and is documented rather than implied.

## Production propagation observation — 2026-08-21

Commit `7675c41` was pushed to the authorized `Coreweaver-Labs-Inc/coreweaver_` `main` branch. Two direct checks of `https://coreweaverlabs.com/` still returned the legacy deployment, including retired AURE Swarm, BFT, VaaS, cryptographic-log, compliance, and production-status language. The public domain has therefore **not yet propagated the pushed flagship commit**. The repository release candidate is validated; production should not be described as updated until the configured deployment target reports the new home, brass-and-teal identity, and `/source-map` route.

## Production release confirmation — 2026-08-21

Vercel subsequently confirmed production deployment `dpl_FTVicNsfs75yytbkDN4nJobdVucB` as `READY`, aliased to `coreweaverlabs.com`, and tied to commit `5542f5f93ec33d322902bdd13f40cbdf6ebe36b3` from `Coreweaver-Labs-Inc/coreweaver_` `main`. The direct deployment URL and a cache-busting custom-domain check both served the new flagship, including the brass-and-teal identity and public **Sources** navigation. The earlier legacy result was a stale browser-cache observation, not a failed deployment. The live release is now confirmed.
